# Generated by `medium-archive hugo`; do not edit.
# Site title/description/intro come from <out>/site.json.
baseURL = $base_url
title = $title
defaultContentLanguage = $locale
locale = $locale
# robots.txt from layouts/robots.txt: the sitemap's address, or a
# blanket disallow when site.json sets "noindex" (a top-level key: it
# has to come before the first [table])
enableRobotsTXT = true

# author terms are the names as written ("yuvipanda"), not title-cased
capitalizeListTitles = false

[pagination]
pagerSize = 24

# a feed announces new posts; the site itself is the archive
[services.rss]
limit = 20

[taxonomies]
tag = "tags"
author = "authors"

$params

[markup.goldmark.renderer]
# some old bodies carry fragments of raw HTML; render them
unsafe = true
