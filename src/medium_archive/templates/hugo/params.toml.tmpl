# This site's data, read by Hugo as site.Params. Written from site/site.toml
# on every export; an unset key is commented out beside an example value.
# The address, name and language are in hugo.toml, the landing page's blurb
# in content/_index.md, and tag and author names in data/tags.yaml and
# data/authors.yaml.

$params
