# In "Mamba meets JupyterLite", the links on "Emscripten toolchain" and
# "Emscripten" carry the placeholder "http://Emscripten Toolchain" as
# their URL, authored that way on Medium. Hugo's URL parser rejects it.
# Point them at the Emscripten site. Two occurrences: the same href in
# the page's embedded editor state and in the rendered HTML. (Replacing
# an href value in the state is safe -- markup offsets index the
# paragraph text, which is untouched.)
file: 88ef49ac4dc8/page.html
count: 2
old-regex: http:(\\u002F\\u002F|//)Emscripten Toolchain
new: https://emscripten.org/
